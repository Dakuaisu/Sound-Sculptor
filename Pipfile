[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = ">=3.0"
flask-cors = ">=4.0"
spotipy = ">=2.23"
python-dotenv = ">=1.0"
joblib = ">=1.3"
numpy = ">=1.24"
pandas = ">=2.0"
scikit-learn = ">=1.3"
openai = ">=1.0"
gunicorn = ">=21.0"

[dev-packages]

[requires]
python_version = "3.10"
